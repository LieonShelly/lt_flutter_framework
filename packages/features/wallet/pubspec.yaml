name: wallet
description: Wallet domain module
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.1.0
  riverpod_annotation: ^4.0.0
  
  # Internal dependencies
  lt_uicomponent:
    path: ../../core/lt_uicomponent
  
  # Domain & Data dependencies
  wallet_domain:
    path: ../../domain/wallet_domain
  wallet_data:
    path: ../../data/wallet_data

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.8
  riverpod_generator: ^4.0.0+1
