name: booking
description: Add Answer domain module
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.4.1
  flutter_localizations:
    sdk: flutter
  flutter_svg: ^2.0.9
  freezed_annotation: ^3.1.0
  go_router: ^17.0.0
  google_fonts: ^6.2.1
  intl: ^0.20.2
  json_annotation: ^4.9.0
  logging: ^1.3.0
  provider: ^6.1.2
  share_plus: ^10.1.3
  booking_data:
    path: ../../data/booking_data
  booking_domain:
    path: ../../domain/booking_domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  analysis_defaults:
    path: ../../core/analysis_defaults
  mocktail_image_network: ^1.2.0
  mocktail: ^1.0.4
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.0+1
  freezed: ^3.2.3
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
