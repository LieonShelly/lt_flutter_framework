name: copilot
description: Copilot/Chat domain module
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.1.0
  
  # Internal dependencies
  lt_network:
    path: ../../core/network
  lt_uicomponent:
    path: ../../core/lt_uicomponent
  feature_core:
    path: ../feature_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
