name: wallet_data
description: Wallet data layer with repositories and data sources
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  wallet_domain:
    path: ../../domain/wallet_domain
  lt_network:
    path: ../../core/network
  
  # Riverpod for Dependency Injection
  flutter_riverpod: ^3.1.0
  riverpod_annotation: ^4.0.0
  
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  lt_annotation:
    path: ../../utls/lt_annotation

dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^3.2.3
  json_serializable: ^6.8.0
  test: ^1.24.0
