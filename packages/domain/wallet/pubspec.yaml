name: wallet
description: Wallet domain module
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.1.0
  
  # Internal dependencies
  lt_uicomponent:
    path: ../../core/lt_uicomponent
  chain_service:
    path: ../../service/chain_service

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
