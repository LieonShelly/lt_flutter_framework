name: booking_domain
description: Reflection domain layer with business logic and entities
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  intl: ^0.20.2
  freezed_annotation: ^3.1.0
  common:
    path: '../../utls/common'
  logging: ^1.3.0
  share_plus: ^10.1.3
  date_utl:
    path: '../../utls/date_utl'

dev_dependencies:
  build_runner: ^2.4.13
  test: ^1.24.0
  freezed: ^3.2.3
